/** Public URL path where the service worker is served */
export const SW_PUBLIC_PATH = '/firebase-messaging-sw.js';

/** Source file path relative to project root (used by dev server plugin) */
export const SW_SOURCE_ENTRY =
  'src/features/push/service-worker/firebase-messaging-sw.ts';
